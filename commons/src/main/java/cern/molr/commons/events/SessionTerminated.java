package cern.molr.commons.events;

import cern.molr.commons.api.response.MissionEvent;

/**
 * Event sent back by the supervisor when the session is terminated
 * When the MoleRunner is destroyed for example
 *
 * @author yassine-kr
 */
public class SessionTerminated implements MissionEvent {

}
